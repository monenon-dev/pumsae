# FastAPI (로컬 uvicorn / 배포 시 Railway 공개 URL)
NEXT_PUBLIC_API_URL=http://localhost:8000

# 배포 시 Vercel 프론트 주소 (OG 이미지 절대경로). 로컬은 생략 가능.
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Neon Postgres 는 프론트가 아니라 backend/.env 의 DATABASE_URL 을 사용합니다.
